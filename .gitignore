# Secrets - never commit
.env

# Gitea data
gitea/*

# MariaDB data
gitea_db/*
!gitea_db/custom.cnf
!gitea_db/backup.conf

# Traefik ACME
traefik/acme.json

# Traefik certificates (user-provided, never commit)
traefik/certs/*.crt
traefik/certs/*.key
traefik/certs/*.toml
